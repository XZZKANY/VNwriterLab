export interface Character {
  id: string;
  projectId: string;
  name: string;
  identity: string;
  appearance: string;
  personality: string;
  goal: string;
  secret: string;
  routeId: string | null;
  notes: string;
}
